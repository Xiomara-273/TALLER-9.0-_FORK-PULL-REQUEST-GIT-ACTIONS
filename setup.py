from setuptools import setup, find_packages

setup(
    name="sistema_inventario",
    version="0.1.0",
    packages=find_packages(),
    description="Proyecto colaborativo para clase de desarrollo",
)